{
  "name": "baseline-oracle",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm -r run dev"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "acorn": "^8.15.0",
    "fast-glob": "^3.3.3",
    "picocolors": "^1.1.1",
    "postcss": "^8.5.6",
    "postcss-selector-parser": "^7.1.0",
    "zod": "^4.1.11"
  }
}
